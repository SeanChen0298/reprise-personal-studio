# Hoist react and react-native to the workspace root so Metro can find a single copy.
# Without this, pnpm's isolated node_modules causes "Invalid hook call" in React Native.
public-hoist-pattern[]=react
public-hoist-pattern[]=react-native
public-hoist-pattern[]=react-dom
