# Reprise is fully local / offline — no environment variables are required.
#
# Earlier versions used VITE_SUPABASE_* (cloud database/auth) and
# VITE_GOOGLE_DRIVE_* (Drive sync); those integrations have been removed.
